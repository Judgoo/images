build:
  - nasm -f elf64 -o a.o main.asm
  - ld -o a.out a.o
run: ./a.out
testdata:
  - "../helloworld.in::../helloworld.out"
runner_args:
  memory_limit: 20480
  cpu_time_limit: 2000
  real_time_limit: 5000
  memory_check_only: true
